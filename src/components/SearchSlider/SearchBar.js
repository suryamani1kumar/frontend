import React from 'react';

const SearchBar = () => {
  return (
    <div>
      <input
        placeholder="emailid"
        type="email"
        style={{ border: '1px soild red' }}
      />
    </div>
  );
};

export default SearchBar;
